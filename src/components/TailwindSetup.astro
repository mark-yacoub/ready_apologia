---
// This pre-compiled stylesheet replaces the Play CDN for performance and eliminates console warnings.
const base = import.meta.env.BASE_URL === '/' ? '' : import.meta.env.BASE_URL;
---
<link rel="stylesheet" href={`${base}/cr-tailwind.css`} />
