---
import Layout from '../../layouts/Layout.astro';
import EvidenceExplorer from '../../components/EvidenceExplorer.jsx';
import { loadAllEvidenceData } from '../../utils/evidenceLoader.js';

const evidenceData = loadAllEvidenceData();

const pageDesc = "Comprehensive directory of Christian apologetics evidence. Explore Biblical Scripture proofs and early Church Patristic testimonies defending core doctrines like the Divinity of Christ.";
const pageKeywords = "verses divinity of christ, bible verses divinity of christ, apologetics evidence, scripture evidence ready apologia, ante-nicene fathers divinity of christ";

const schemaObj = {
  "@context": "https://schema.org",
  "@type": "CollectionPage",
  "headline": "Christian Apologetics Evidence & Scripture Evidence",
  "description": pageDesc,
  "hasPart": evidenceData.map(t => ({
    "@type": "WebPage",
    "name": t.evidenceId.replace(/_/g, ' ').replace(/\b\w/g, l => l.toUpperCase()),
    "url": `${Astro.url.origin}${import.meta.env.BASE_URL === '/' ? '' : import.meta.env.BASE_URL}/evidence/${t.evidenceId}`
  }))
};
const schemaJson = JSON.stringify(schemaObj);
---

<Layout title="Apologetics Evidence" description={pageDesc} keywords={pageKeywords} schema={schemaJson}>
  <div class="evidence-index-container">
    <EvidenceExplorer client:load evidence={evidenceData} />
  </div>
</Layout>

<style>
  .evidence-index-container {
    display: flex;
    flex-direction: column;
    padding-top: 12px;
  }
</style>
